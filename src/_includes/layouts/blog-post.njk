<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg+xml" href="{{ "/images/logo.svg" | url }}">
    <title>{{ title }} - ArbInq</title>
    <meta name="description" content="{{ title }}">
    <link rel="stylesheet" href="{{ "/css/base.css" | url }}">
    <link rel="stylesheet" href="{{ "/css/main.css" | url }}">
</head>
<body>
    <header class="header">
        <nav class="nav container">
            <a href="{{ "/" | url }}" class="logo">
                <img src="{{ "/images/logo.svg" | url }}" alt="ArbInq" height="48">
            </a>
            <ul class="nav-links">
                <li><a href="{{ "/" | url }}#products">Products</a></li>
                <li><a href="{{ "/about/" | url }}">About</a></li>
                <li><a href="{{ "/blog/" | url }}">Blog</a></li>
                <li class="dropdown">
                    <a href="#">Solutions</a>
                    <ul class="dropdown-menu">
                        {% for icpItem in site.icps %}
                        {% set slugPath = "/" + icpItem.slug + "/" %}
                        <li><a href="{{ slugPath | url }}">{{ icpItem.label }}</a></li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ "/contact/" | url }}">Contact</a></li>
            </ul>
            <a href="https://cal.com/ateesdalejr/init" class="btn btn-primary">Book Audit</a>
            <button class="theme-toggle" id="theme-toggle" aria-label="Toggle light/dark mode" title="Toggle light/dark mode">
                <svg class="icon-sun" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>
                <svg class="icon-moon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
            </button>
        </nav>
    </header>

    <main>
        <section class="page-hero">
            <div class="container">
                {% if topics %}
                <div class="post-topics">
                    {% for topic in topics %}
                    <span class="topic-tag">{{ topic }}</span>
                    {% endfor %}
                </div>
                {% endif %}
                <h1>{{ title }}</h1>
                <p class="post-meta">{{ date | readableDate }} &nbsp;&middot;&nbsp; {{ author }}</p>
            </div>
        </section>

        <section>
            <div class="container">
                <div class="post-body">
                    {{ content | safe }}
                </div>
                <div class="post-back">
                    <a href="{{ "/blog/" | url }}" class="btn btn-secondary">&larr; All Posts</a>
                </div>
            </div>
        </section>

        <section class="cta">
            <div class="container">
                <h2>Found Something Worth Looking At?</h2>
                <p>Book a call and we'll scope your audit on the spot.</p>
                <a href="https://cal.com/ateesdalejr/init" class="btn btn-primary btn-lg">Book Your Audit</a>
            </div>
        </section>
    </main>

    <footer class="site-footer"></footer>
    <script src="{{ "/js/theme-toggle.js" | url }}"></script>
    <script src="{{ "/js/footer.js" | url }}"></script>
</body>
</html>
