<header class="header">
    <nav class="nav container">
        <a href="{{ "/" | url }}" class="logo">ArbInq</a>
        <ul class="nav-links">
            {% if navType == "device-inventory" %}
            <li><a href="{{ "/products/device-inventory/" | url }}">Product</a></li>
            <li><a href="{{ "/products/device-inventory/" | url }}#pricing">Pricing</a></li>
            {% else %}
            <li><a href="#products">Products</a></li>
            <li><a href="{{ "/about/" | url }}">About</a></li>
            {% endif %}
            <li><a href="{{ "/blog/" | url }}">Blog</a></li>
            <li class="dropdown">
                <a href="#">Solutions</a>
                <ul class="dropdown-menu">
                    {% for icpItem in site.icps %}
                    {% set slugPath = "/" + icpItem.slug + "/" %}
                    <li><a href="{{ slugPath | url }}">{{ icpItem.label }}</a></li>
                    {% endfor %}
                </ul>
            </li>
            <li><a href="mailto:andrew@arbinquiry.com">Contact</a></li>
        </ul>
        {% if icp.nav.cta_text and not (navType == "device-inventory" and icp.nav.di_no_cta) %}
        <a href="{{ icp.nav.cta_href }}" class="btn btn-primary">{{ icp.nav.cta_text }}</a>
        {% endif %}
    </nav>
</header>
